
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Mantenedores!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('public/css/style.css') }}">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="container">
            <header>
                {% block seccion2 %}{% endblock %}
            </header>
            <nav>
                {% block seccion1 %}
                    
                        <ul>
                            <div id="sidebar">
                                {{ render(controller('MRFUsuarioBundle:Menu:list')) }}
                            </div>                          
                        </ul>
                        
                    {#<ul>
                        <li><a rel="stylesheet" href="{{ path('mrf_cliente_list') }}">Clientes</a></li>
                        <li><a rel="stylesheet" href="{{ path('mrf_proveedor_list') }}">Proveedores</a></li>
                        <li><a rel="stylesheet" href="{{ path('mrf_usuario_list') }}">Usuarios</a></li>
                    </ul>#}   
                    
                {% endblock %}
            </nav>
            <article>{% block seccion3 %}
                {% for flashMessage in app.session.flashbag.get('mensaje') %}
                <strong>
                <div class="alert" value="alert">
                        <div class ="container">{{ flashMessage }}</div>
                    </div>
                    </strong>
                {% endfor %}
                {% endblock %}</article>
        </div>
        
    </body>
</html>
