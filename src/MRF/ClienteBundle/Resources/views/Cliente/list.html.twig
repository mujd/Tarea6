{% extends '::layout.html.twig' %} 

{% block seccion2 %}<h1 class="titulo-blog">Clientes</h1>{% endblock %}

{% block seccion3 %}
    <link rel="stylesheet" href="{{ asset('public/css/style.css') }}"/>

    <h2>Listado de Clientes</h2><hr>

    <a href="{{ path('mrf_cliente_add') }}" class="btn btn-info" role="button">Agregar Cliente</a><br><br>
    {#
    <a href="#" class="glyphicon glyphicon-plus btn btn-info btn-lg" style="visible:false;" onclick="registrar(0)">Agregar Cliente</a><br><br>
    
    <button  id='btnCompetencia' type="button" class="btn btn-sm btn-warning" onclick="registrar(0)">Agregar Cliente</button>
    #}


    <div class="row table-responsive col-lg-10">
        {% for message in app.session.flashbag().get('mensaje') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}


        <table id="example" class="table table-bordered table-condensed table-hover display">

            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombres</th>
                    <th>Apellido Paterno</th>
                    <th>Apellido Materno</th>
                    <th>Rut</th>
                    <th>Dirección</th>
                    <th>Email</th>
                    <th>Telefono</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for Cliente in cliente %}
                    <tr>
                        <td>{{ Cliente.id }}</td>
                        <td>{{ Cliente.nombres }}</td>
                        <td>{{ Cliente.apellidoP }}</td>
                        <td>{{ Cliente.apellidoM }}</td>
                        <td>{{ Cliente.rut }}</td>
                        <td>{{ Cliente.direccion }}</td>
                        <td>{{ Cliente.email }}</td>
                        <td>{{ Cliente.fono }}</td>
                        <td>
                            {% if usuarioRol == 'Administrador' %}
                                <a class="glyphicon glyphicon-pencil" href="{{ path('mrf_cliente_edit', {id: Cliente.id}) }}"></a>
                                <a class="glyphicon glyphicon-trash edel" href="{{ path('mrf_cliente_delete', {id: Cliente.id}) }}"></a>                            
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}                
            </tbody>
        </table>
    </div>

{% endblock %}

{% block javascript %}
    <script type="text/javascript">


        function registrar() {



            alert("OK");
        }


    </script>

    <script src="{{ asset('public/js/mae/maeFunciones.js') }}" type="text/javascript"></script>
    <script src="{{ asset('public/js/comFunciones.js') }}" type="text/javascript"></script>
    <script src="{{ asset('public/js/dhtmlxSuite_v50_std/codebase/dhtmlx.js') }}" type="text/javascript"></script>
{% endblock %}
